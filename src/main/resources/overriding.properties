# Put here properties that will be used for local debugging and local test runs.
# The properties defined in this file override any properties set in dedicated folder
property-mapper.prefix-separator=.
property-mapper.naming-strategy=KEBAB_CASE

location.zone-id=#{T(java.time.ZoneId).systemDefault().toString()}

system.rally.enable=true

system.rp.enable=false
